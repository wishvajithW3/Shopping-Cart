import { Link, useLocation } from "react-router-dom";
export default function CheckoutSuccess(){ const {state}=useLocation(); const order=state?.order; return <main><h1>Checkout Summary</h1>{!order?<p>No order found. <Link to="/">Go shopping</Link></p>:<div className="summary-box"><h2>Order Created</h2><p>Status: {order.status}</p>{order.items.map(i=><div className="cart-row" key={i.product}><img src={i.image}/><div><h3>{i.name}</h3><p>{i.quantity} x Rs. {i.price}</p></div><strong>Rs. {i.quantity*i.price}</strong></div>)}<h2>Total: Rs. {order.totalPrice}</h2><p>Payment gateway is future enhancement, so this page shows the order summary before payment.</p><Link to="/">Continue Shopping</Link></div>}</main> }
